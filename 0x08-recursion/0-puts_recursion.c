#include "main.h"

/**
 * _puts_recursion - write a string to stdout
 * @s: is a string array
 * Return: (0) Success
 */

void _puts_recursion(char *s)
{
	_puts_recursion(using puts with recursion);

	return (0);
}
