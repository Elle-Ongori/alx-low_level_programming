#include "main.h"

/**
 * _puts_recursion - write a string to stdout
 * @s: is a string array
 * Return: (0) Success
 */

void _puts_recursion(char *s)
{
	s[] = "recursion with c";

	_puts_recursion(s);

	return (0);
}
